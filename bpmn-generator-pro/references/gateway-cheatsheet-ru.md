# Шпаргалка по BPMN Шлюзам (Gateways)

Краткое руководство по правильному использованию шлюзов в BPMN 2.0

---

## 🔷 Типы шлюзов (5 штук)

| Тип | Символ | Когда использовать | Поведение |
|-----|--------|-------------------|-----------|
| **Exclusive (XOR)** | ◇ пустой | Выбор ОДНОГО пути | Только один путь активируется |
| **Parallel (AND)** | ◇ с + | Параллельная работа | ВСЕ пути выполняются |
| **Inclusive (OR)** | ◇ с ○ | Условное ветвление | ОДИН ИЛИ НЕСКОЛЬКО путей |
| **Event-Based** | ◇ с ⬡ | Ожидание события | Первое событие определяет путь |
| **Complex** | ◇ с * | Сложная логика | Не поддерживается движками! |

---

## 1️⃣ Exclusive Gateway (XOR) - Решение

### Обязательные правила ✅

```xml
<bpmn:exclusiveGateway id="Gateway_Decision"
  name="Сумма > 1000?"  <!-- ✅ Вопрос! -->
  default="Flow_Small">  <!-- ✅ ОБЯЗАТЕЛЬНО default! -->
  <bpmn:outgoing>Flow_Large</bpmn:outgoing>
  <bpmn:outgoing>Flow_Small</bpmn:outgoing>
</bpmn:exclusiveGateway>

<bpmn:sequenceFlow id="Flow_Large" name="Да">
  <bpmn:conditionExpression>${amount > 1000}</bpmn:conditionExpression>
</bpmn:sequenceFlow>

<bpmn:sequenceFlow id="Flow_Small" name="Нет"
  sourceRef="Gateway_Decision" />  <!-- Без условия = default -->
```

### Что ВСЕГДА делать:
- ✅ Именовать как **вопрос**: "Одобрено?", "Сумма > 1000?"
- ✅ Устанавливать **default flow** (атрибут `default="Flow_ID"`)
- ✅ Делать условия **взаимоисключающими** (только одно может быть true)
- ✅ Покрывать **все возможные значения** (полнота)

### Критические ошибки ❌

#### ❌ Нет default flow → DEADLOCK
```xml
<!-- ОШИБКА: Если risk='MEDIUM', ни одно условие не сработает -->
<bpmn:exclusiveGateway id="Gateway_Risk">
  <bpmn:outgoing>Flow_High</bpmn:outgoing>  <!-- ${risk == 'HIGH'} -->
  <bpmn:outgoing>Flow_Low</bpmn:outgoing>   <!-- ${risk == 'LOW'} -->
</bpmn:exclusiveGateway>
<!-- Процесс зависнет! Нужен default flow -->
```

#### ❌ Перекрывающиеся условия → Недетерминизм
```xml
<!-- ОШИБКА: Оба условия могут быть true одновременно -->
<bpmn:conditionExpression>${age < 25}</bpmn:conditionExpression>  <!-- Студент -->
<bpmn:conditionExpression>${loyaltyYears > 5}</bpmn:conditionExpression>  <!-- VIP -->
<!-- Что если age=22 И loyaltyYears=6? Какой путь выберется? -->
```

#### ❌ Неполные условия → Пропущены случаи
```xml
<!-- ОШИБКА: Не покрывает возраст >= 65 -->
<bpmn:conditionExpression>${age < 18}</bpmn:conditionExpression>  <!-- Ребёнок -->
<bpmn:conditionExpression>${age >= 18 && age < 65}</bpmn:conditionExpression>  <!-- Взрослый -->
<!-- А где пенсионеры?! -->
```

---

## 2️⃣ Parallel Gateway (AND) - Параллелизм

### Обязательные правила ✅

```xml
<!-- РАЗВЕТВЛЕНИЕ (Fork): Создаёт несколько токенов -->
<bpmn:parallelGateway id="Gateway_Fork" name="Начать проверки">
  <bpmn:outgoing>Flow_ToCredit</bpmn:outgoing>
  <bpmn:outgoing>Flow_ToIncome</bpmn:outgoing>
  <bpmn:outgoing>Flow_ToCollateral</bpmn:outgoing>
</bpmn:parallelGateway>
<!-- НЕТ условий! Все 3 пути ВСЕГДА выполняются -->

<!-- СИНХРОНИЗАЦИЯ (Join): Ждёт ВСЕ токены -->
<bpmn:parallelGateway id="Gateway_Join" name="Синхронизация">
  <bpmn:incoming>Flow_CreditDone</bpmn:incoming>
  <bpmn:incoming>Flow_IncomeDone</bpmn:incoming>
  <bpmn:incoming>Flow_CollateralDone</bpmn:incoming>
  <bpmn:outgoing>Flow_AllComplete</bpmn:outgoing>
</bpmn:parallelGateway>
```

### Что ВСЕГДА делать:
- ✅ Использовать **парами**: Fork → ... → Join
- ✅ **Одинаковое количество** путей (3 выхода → 3 входа)
- ✅ **НИКАКИХ условий** на исходящих потоках
- ✅ **Обязательная синхронизация** (избегать multi-merge)

### Критические ошибки ❌

#### ❌ Несбалансированные типы → DEADLOCK!
```xml
<!-- ОШИБКА: Parallel fork → Exclusive join -->
<bpmn:parallelGateway id="Gateway_Fork">  <!-- Создаёт 3 токена -->
  <bpmn:outgoing>Flow_A</bpmn:outgoing>
  <bpmn:outgoing>Flow_B</bpmn:outgoing>
  <bpmn:outgoing>Flow_C</bpmn:outgoing>
</bpmn:parallelGateway>

<bpmn:exclusiveGateway id="Gateway_BadJoin">  <!-- ❌ Ждёт 1 токен! -->
  <bpmn:incoming>Flow_A</bpmn:incoming>
  <bpmn:incoming>Flow_B</bpmn:incoming>
  <bpmn:incoming>Flow_C</bpmn:incoming>
</bpmn:exclusiveGateway>
<!-- Результат: Первый токен проходит, 2 застревают → DEADLOCK -->
```

#### ❌ Отсутствие синхронизации (Multi-Merge) → Двойное выполнение
```xml
<!-- ОШИБКА: Нет join gateway -->
<bpmn:parallelGateway id="Gateway_Fork">
  <bpmn:outgoing>Flow_TaskA</bpmn:outgoing>
  <bpmn:outgoing>Flow_TaskB</bpmn:outgoing>
</bpmn:parallelGateway>

<!-- Обе задачи ведут прямо к следующей задаче -->
<bpmn:sequenceFlow sourceRef="Task_A" targetRef="Task_Final" />
<bpmn:sequenceFlow sourceRef="Task_B" targetRef="Task_Final" />
<!-- ❌ Task_Final получит 2 токена → выполнится ДВАЖДЫ! -->

<!-- ИСПРАВЛЕНИЕ: Добавить parallel join перед Task_Final -->
```

#### ❌ Несовпадающее количество путей → DEADLOCK
```xml
<!-- ОШИБКА: 3 пути выхода, 2 пути входа -->
<bpmn:parallelGateway id="Gateway_Fork">
  <bpmn:outgoing>Flow_A</bpmn:outgoing>
  <bpmn:outgoing>Flow_B</bpmn:outgoing>
  <bpmn:outgoing>Flow_C</bpmn:outgoing>  <!-- 3 токена создано -->
</bpmn:parallelGateway>

<bpmn:parallelGateway id="Gateway_Join">
  <bpmn:incoming>Flow_A</bpmn:incoming>
  <bpmn:incoming>Flow_B</bpmn:incoming>
  <!-- Нет Flow_C! → Join ждёт 3 токена вечно → DEADLOCK -->
</bpmn:parallelGateway>
```

---

## 3️⃣ Inclusive Gateway (OR) - Условный параллелизм

### Обязательные правила ✅

```xml
<bpmn:inclusiveGateway id="Gateway_ReviewTypes"
  name="Какие проверки нужны?">
  <bpmn:outgoing>Flow_Legal</bpmn:outgoing>
  <bpmn:outgoing>Flow_Financial</bpmn:outgoing>
  <bpmn:outgoing>Flow_Technical</bpmn:outgoing>
</bpmn:inclusiveGateway>

<bpmn:sequenceFlow id="Flow_Legal" name="Есть договор">
  <bpmn:conditionExpression>${hasContract}</bpmn:conditionExpression>
</bpmn:sequenceFlow>
<bpmn:sequenceFlow id="Flow_Financial" name="Сумма > $10k">
  <bpmn:conditionExpression>${amount > 10000}</bpmn:conditionExpression>
</bpmn:sequenceFlow>
<!-- Default: гарантирует хотя бы один путь -->
<bpmn:sequenceFlow id="Flow_Technical" name="Новая технология" default="true" />
```

### Что ВСЕГДА делать:
- ✅ Гарантировать **хотя бы один путь** (default flow)
- ✅ **НЕ завершать токены** между OR-split и OR-join (никаких End!)
- ✅ Совпадение путей split и join
- ⚠️ **ИЗБЕГАТЬ в сложных моделях** (лучше Parallel + Exclusive)

### Критические ошибки ❌

#### ❌ Завершение токена между split/join → DEADLOCK!
```xml
<!-- ОШИБКА: End event в пути OR -->
<bpmn:inclusiveGateway id="Gateway_Split">
  <bpmn:outgoing>Flow_ProcessA</bpmn:outgoing>
  <bpmn:outgoing>Flow_ProcessB</bpmn:outgoing>
</bpmn:inclusiveGateway>

<!-- Путь A завершается рано -->
<bpmn:sequenceFlow sourceRef="Task_A" targetRef="End_PathA" />  <!-- ❌ -->

<!-- Путь B идёт к join -->
<bpmn:inclusiveGateway id="Gateway_Join">
  <bpmn:incoming>Flow_FromA</bpmn:incoming>
  <bpmn:incoming>Flow_FromB</bpmn:incoming>
  <!-- Ждёт вечно токен от Path A, который уже завершён! → DEADLOCK -->
</bpmn:inclusiveGateway>
```

#### ❌ Нет default пути → Возможен DEADLOCK
```xml
<!-- ОШИБКА: Оба условия могут быть false -->
<bpmn:inclusiveGateway id="Gateway_Optional">
  <bpmn:outgoing>Flow_A</bpmn:outgoing>
  <bpmn:outgoing>Flow_B</bpmn:outgoing>
</bpmn:inclusiveGateway>
<bpmn:sequenceFlow id="Flow_A">
  <bpmn:conditionExpression>${typeA}</bpmn:conditionExpression>
</bpmn:sequenceFlow>
<bpmn:sequenceFlow id="Flow_B">
  <bpmn:conditionExpression>${typeB}</bpmn:conditionExpression>
</bpmn:sequenceFlow>
<!-- Если typeA=false И typeB=false → НИ ОДИН путь не активируется → DEADLOCK -->
<!-- ИСПРАВЛЕНИЕ: Пометить один поток как default="true" -->
```

---

## 4️⃣ Правила балансировки шлюзов

### ✅ Безопасные паттерны:
```
Exclusive Split → Exclusive Merge ✅
Parallel Split → Parallel Join ✅
Inclusive Split → Inclusive Join ✅
```

### ❌ ОПАСНЫЕ паттерны (DEADLOCK!):
```
Parallel Split → Exclusive Join ❌
  Причина: Join получает N токенов, ожидает 1

Exclusive Split → Parallel Join ❌
  Причина: Join ожидает N токенов, получает 1

Inclusive Split → Parallel Join ❌
  Причина: Join может получить неправильное количество токенов
```

### ⚠️ Когда асимметрия допустима:
```
Parallel Split (3 пути) → 1 путь завершается рано → Parallel Join (2 пути) ✅
  ЕСЛИ задокументировано и намеренно (например, отправка уведомления независимо)
```

---

## 5️⃣ Универсальные анти-паттерны

### ❌ Неявный шлюз (Implicit Gateway) - КРИТИЧНО!

**Проблема:** Логика решения скрыта в потоках вместо явного шлюза

```xml
<!-- НЕПРАВИЛЬНО: Задача с условными исходящими потоками -->
<bpmn:userTask id="Task_RequestDocs">
  <bpmn:outgoing>Flow_Continue</bpmn:outgoing>
  <bpmn:outgoing>Flow_Cancel</bpmn:outgoing>  <!-- ❌ Два исходящих! -->
</bpmn:userTask>

<bpmn:sequenceFlow id="Flow_Cancel" sourceRef="Task_RequestDocs">
  <bpmn:conditionExpression>${cancelled}</bpmn:conditionExpression>  <!-- ❌ -->
</bpmn:sequenceFlow>
```

**ПРАВИЛЬНО:**
```xml
<!-- Задача имеет ОДИН исходящий поток -->
<bpmn:userTask id="Task_RequestDocs">
  <bpmn:outgoing>Flow_ToGateway</bpmn:outgoing>  <!-- ✅ -->
</bpmn:userTask>

<!-- Явный шлюз для решения -->
<bpmn:exclusiveGateway id="Gateway_Cancel" name="Отменить?"
  default="Flow_Continue">
  <bpmn:outgoing>Flow_Continue</bpmn:outgoing>
  <bpmn:outgoing>Flow_Cancel</bpmn:outgoing>
</bpmn:exclusiveGateway>
```

**Правило обнаружения:**
- Если у задачи/активности **больше одного исходящего потока** → ОШИБКА НЕЯВНОГО ШЛЮЗА
- Исключение: Boundary events (ошибки/таймауты) допустимы

### ❌ Шлюз отправляет сообщение
```
Gateway → Message Event  ❌ НЕТ!
Task → Message Event     ✅ ДА!
```
**Причина:** Только активности могут отправлять/получать сообщения

### ❌ Multi-Merge (множественное слияние) - ANTI-PATTERN

**Проблема:** Множественные потоки сходятся напрямую в задачу БЕЗ явного merge gateway

#### 🔴 ОПАСНЫЙ Multi-Merge (после Parallel)
```xml
<!-- НЕПРАВИЛЬНО: Parallel fork без синхронизации -->
<bpmn:parallelGateway id="Gateway_Fork">
  <bpmn:outgoing>Flow_ToA</bpmn:outgoing>
  <bpmn:outgoing>Flow_ToB</bpmn:outgoing>
</bpmn:parallelGateway>

<!-- Оба потока идут напрямую в финальную задачу -->
<bpmn:sequenceFlow sourceRef="Task_A" targetRef="Task_Final" />
<bpmn:sequenceFlow sourceRef="Task_B" targetRef="Task_Final" />
<!-- ❌ Task_Final получает 2 токена → выполняется ДВАЖДЫ! -->
```

**Последствия:**
- Task выполняется несколько раз (по числу токенов)
- Создаются дубликаты данных
- Race conditions
- Непредсказуемое поведение

**ПРАВИЛЬНО:**
```xml
Parallel Fork → Task A ──┐
             → Task B ──┴→ Parallel Join → Task_Final ✅
                          (синхронизация)
```

#### ⚠️ Технически допустимый, НО НЕ рекомендуется

```xml
<!-- После Exclusive гateways (только один токен может прийти) -->
<bpmn:userTask id="Task_NotifyRejection">
  <bpmn:incoming>Flow_FromXOR1</bpmn:incoming>  <!-- путь 1 -->
  <bpmn:incoming>Flow_FromXOR2</bpmn:incoming>  <!-- путь 2 -->
  <bpmn:incoming>Flow_FromXOR3</bpmn:incoming>  <!-- путь 3 -->
</bpmn:userTask>
<!-- ⚠️ Технически OK, но неясно и сложно поддерживать -->
```

**Почему это плохо:**
- ❌ Неясно, что потоки взаимоисключающие
- ❌ Сложно добавлять новые пути
- ❌ Риск ошибки при изменениях
- ❌ Нарушает best practice "одна входящая стрелка"

**ЛУЧШАЯ ПРАКТИКА - Явный Merge Gateway:**
```xml
<bpmn:exclusiveGateway id="Gateway_MergeRejections">
  <bpmn:incoming>Flow_FromXOR1</bpmn:incoming>
  <bpmn:incoming>Flow_FromXOR2</bpmn:incoming>
  <bpmn:incoming>Flow_FromXOR3</bpmn:incoming>
  <bpmn:outgoing>Flow_ToNotify</bpmn:outgoing>
</bpmn:exclusiveGateway>

<bpmn:userTask id="Task_NotifyRejection">
  <bpmn:incoming>Flow_ToNotify</bpmn:incoming>  <!-- ✅ Один вход -->
</bpmn:userTask>
```

**Преимущества:**
- ✅ Визуально понятно (явная точка слияния)
- ✅ Легко поддерживать
- ✅ Невозможна ошибка multi-token
- ✅ Соответствует стандартам

#### 📏 Золотое правило Multi-Merge

**Правило:** Только **шлюзы** могут иметь несколько входящих потоков

```
✅ Gateway с несколькими входами → OK (это их цель)
❌ Task с несколькими входами → НЕ OK (сначала merge gateway!)
❌ Start Event с несколькими входами → ОШИБКА (невалидно)
✅ End Event с несколькими входами → OK (каждый токен завершается)
```

**Паттерн для запоминания:**
```
Несколько путей → Merge Gateway → Один поток → Task
```

#### ⚙️ ОБЯЗАТЕЛЬНЫЙ Алгоритм предотвращения Multi-Merge

**КРИТИЧНО:** Выполняй этот алгоритм для КАЖДОГО создаваемого потока.

**Пошаговый процесс:**

```
ДЛЯ КАЖДОГО sequence flow который ты создаёшь:

1. ОПРЕДЕЛИ целевой элемент (куда ведёт поток)

2. ПРОВЕРЬ тип целевого элемента:
   ├─ Это End Event?
   │  └─ ДА → ✅ РАЗРЕШЕНО несколько входов (переход к шагу 6)
   │
   └─ Это Task, Gateway или Start/Intermediate Event?
      └─ ДА → ПРОДОЛЖИТЬ шаг 3

3. ПОСЧИТАЙ существующие входящие потоки:
   ├─ incoming.length === 0
   │  └─ ✅ БЕЗОПАСНО: Добавь поток напрямую (переход к шагу 6)
   │
   └─ incoming.length >= 1
      └─ ⚠️ MULTI-MERGE ОБНАРУЖЕН → ПЕРЕХОД к шагу 4

4. ВСТАВЬ MERGE GATEWAY (ОБЯЗАТЕЛЬНО):

   a) Определи тип merge:
      ├─ Все пути из Parallel Gateway?
      │  └─ Используй Parallel Gateway (синхронизация)
      │
      ├─ Пути взаимоисключающие?
      │  └─ Используй Exclusive Gateway (XOR merge)
      │
      └─ Пути могут пересекаться (один или более активны)?
         └─ Используй Inclusive Gateway (OR merge)

   b) Создай merge gateway:
      - ID: Gateway_Merge[Назначение]  (напр., Gateway_MergeRejections)
      - Name: Описательное имя (напр., "Объединение отказов")
      - Размести ПЕРЕД целевым элементом

   c) Перенаправь ВСЕ входящие потоки:
      - Измени targetRef существующих потоков → merge gateway
      - Измени targetRef нового потока → merge gateway

   d) Создай единственный выходной поток:
      - sourceRef: merge gateway
      - targetRef: исходный целевой элемент

5. ОБНОВИ визуальную раскладку:
   - Размести gateway на 110px левее цели (X - 110)
   - Обнови все waypoints потоков
   - Убедись в чистой визуальной маршрутизации

6. ВАЛИДИРУЙ результат:
   ✅ Целевой элемент имеет РОВНО ОДИН входящий поток
   ✅ Merge gateway (если добавлен) имеет несколько входящих
   ✅ Семантика визуально понятна

ГОТОВО ✅
```

**Дерево решений:**

```
Создаёшь поток к элементу X?
│
├─ X это End Event? → ✅ Добавь поток напрямую
│
├─ У X нет входящих потоков? → ✅ Добавь поток напрямую
│
└─ У X есть 1+ входящих потоков? → ⚠️ СНАЧАЛА ВСТАВЬ MERGE GATEWAY
   │
   ├─ После Parallel split? → Используй Parallel Gateway
   ├─ Взаимоисключающие? → Используй Exclusive Gateway
   └─ Могут пересекаться? → Используй Inclusive Gateway
```

**Пример применения:**

```xml
<!-- СЦЕНАРИЙ: Добавляем 2-й путь к Task_Notify -->

<!-- ДО: Task имеет 1 входящий -->
<bpmn:userTask id="Task_Notify">
  <bpmn:incoming>Flow_Success</bpmn:incoming>  <!-- 1-й поток существует -->
</bpmn:userTask>

<!-- АЛГОРИТМ ОБНАРУЖИВАЕТ: incoming.length = 1 → ВСТАВИТЬ GATEWAY -->

<!-- ПОСЛЕ: Алгоритм автоматически вставляет merge gateway -->
<bpmn:exclusiveGateway id="Gateway_MergeNotify">
  <bpmn:incoming>Flow_Success</bpmn:incoming>      <!-- Перенаправлен -->
  <bpmn:incoming>Flow_Alternative</bpmn:incoming>  <!-- Новый поток -->
  <bpmn:outgoing>Flow_Merge_Notify</bpmn:outgoing>
</bpmn:exclusiveGateway>

<bpmn:userTask id="Task_Notify">
  <bpmn:incoming>Flow_Merge_Notify</bpmn:incoming>  <!-- ✅ Всё ещё 1 входящий -->
</bpmn:userTask>
```

**Частые ошибки:**

```
❌ "Добавлю gateway потом" → НЕТ! Добавь СЕЙЧАС
❌ "Всего 2 потока, не нужен gateway" → НЕВЕРНО! Всё равно нужен
❌ "Потоки exclusive, всё в порядке" → НЕ ОК! Нужен явный merge
❌ "End event может сливать" → ТОЛЬКО для end events, не для задач
```

**Запомни:**
> **"Если собираешься создать 2-й входящий поток к задаче/событию (кроме end event), СТОП и сначала вставь merge gateway."**

---

## 📋 Чеклист валидации шлюзов

### Для каждого Exclusive Gateway:
- [ ] Именован как вопрос?
- [ ] Установлен default flow?
- [ ] Условия взаимоисключающие?
- [ ] Все значения покрыты?
- [ ] Нет условных потоков от задач?

### Для каждого Parallel Gateway:
- [ ] Есть парный join?
- [ ] Одинаковое количество путей (или задокументировано)?
- [ ] Нет условий на исходящих потоках?
- [ ] Нет multi-merge (есть join)?
- [ ] Join ждёт ВСЕ пути?

### Для каждого Inclusive Gateway:
- [ ] Есть default flow или гарантированная активация?
- [ ] Нет End events между split и join?
- [ ] Пути join совпадают с путями split?
- [ ] Сложность оправдана (нельзя упростить)?

### Общие правила (для всех элементов):
- [ ] **Задачи имеют ОДИН входящий поток** (используй merge gateway)
- [ ] **Задачи имеют ОДИН исходящий поток** (используй decision gateway)
- [ ] **НЕТ Multi-Merge** - все слияния через explicit gateway
- [ ] **НЕТ Implicit Gateway** - все решения через явный шлюз
- [ ] **Правило:** Несколько путей → Merge Gateway → Один поток → Task

---

## 🎯 Краткая памятка по токенам

| Шлюз | Split → Токены | Join → Поведение |
|------|----------------|------------------|
| **Exclusive** | 1 → 1 путь | Пропускает сразу (нет ожидания) |
| **Parallel** | 1 → N токенов (все пути) | Ждёт N токенов |
| **Inclusive** | 1 → 1-N токенов (условно) | Ждёт активированные токены |
| **Event-Based** | 1 → 1 путь (первое событие) | N/A (как exclusive) |

---

## 🔗 Источники

Основано на исследовании:
- [Visual Paradigm: BPMN Gateway Types](https://www.visual-paradigm.com/guide/bpmn/bpmn-gateway-types/)
- [Flokzu: Inclusive Gateway Errors](https://docs.flokzu.com/en/article/common-errors-and-recommendations-when-using-inclusive-gateways-and-parallelism-1drj4nh/)
- [BPM Tips: Parallel Gateways](https://bpmtips.com/parallel-gateways-and-and-tokens/)
- [ARIS Community: Gateway Split and Merge](https://ariscommunity.com/users/yong-yeow-ching/2021-03-11-gateway-usage-when-come-split-and-merge)
- [Camunda: BPMN 2.0 Reference](https://camunda.com/bpmn/reference/)

---

## 🚫 PMA Ограничение: Дорожки ЗАПРЕЩЕНЫ для процессов ОДНОЙ организации

**Scope:** Это ограничение применяется к процессам внутри ОДНОЙ организации (одна компания с внутренними отделами).

### Почему дорожки (Lanes) вредны для internal процессов:

- ❌ **Увеличивают размер модели** - диаграмма становится большой и труднее обозримой
- ❌ **Ломают happy path** - успешный сценарий идёт зигзагом вместо прямой линии
- ❌ **Усложняют чтение** - нужно прыгать между дорожками чтобы отследить поток
- ❌ **Снижают читаемость** - визуальная сложность скрывает логику процесса

### ❌ НЕПРАВИЛЬНО (с дорожками):

```xml
<bpmn:process>
  <bpmn:laneSet>
    <bpmn:lane id="Lane_Sales" name="Продажи">
      <bpmn:flowNodeRef>Task_CreateQuote</bpmn:flowNodeRef>
    </bpmn:lane>
    <bpmn:lane id="Lane_Finance" name="Финансы">
      <bpmn:flowNodeRef>Task_ApprovePrice</bpmn:flowNodeRef>
    </bpmn:lane>
    <bpmn:lane id="Lane_Operations" name="Операции">
      <bpmn:flowNodeRef>Task_FulfillOrder</bpmn:flowNodeRef>
    </bpmn:lane>
  </bpmn:laneSet>
</bpmn:process>
```

**Визуальная проблема:**
```
┌──────────────────────────────────────┐
│ Продажи    │ Создать КП → ↓         │
├────────────┼─────────────────────────┤
│ Финансы    │           Утвердить → ↓│  ❌ Зигзаг
├────────────┼─────────────────────────┤
│ Операции   │                Выполнить│
└──────────────────────────────────────┘
```

### ✅ ПРАВИЛЬНО (оверлеи ролей):

**Вместо дорожек:** Указывай роли прямо на задачах через атрибуты Camunda или аннотации.

```xml
<bpmn:process>
  <!-- Без laneSet - плоская структура -->

  <bpmn:userTask id="Task_CreateQuote" name="Создать КП"
                 camunda:candidateGroups="sales">
    <!-- Роль указана в атрибуте -->
  </bpmn:userTask>

  <bpmn:userTask id="Task_ApprovePrice" name="Утвердить цену"
                 camunda:candidateGroups="finance">
  </bpmn:userTask>

  <bpmn:userTask id="Task_FulfillOrder" name="Выполнить заказ"
                 camunda:candidateGroups="operations">
  </bpmn:userTask>
</bpmn:process>
```

**Визуальное преимущество:**
```
Start → Создать КП → Утвердить цену → Выполнить заказ → End
        [Продажи]    [Финансы]         [Операции]           ✅ Прямая линия
```

### 3 способа указать роли:

**Вариант A: Атрибуты Camunda** (рекомендуется для executable)
```xml
<bpmn:userTask name="Проверить заявку"
               camunda:assignee="${reviewer}"
               camunda:candidateGroups="underwriters,managers">
</bpmn:userTask>
```

**Вариант B: Текстовые аннотации** (для документации)
```xml
<bpmn:textAnnotation>
  <bpmn:text>Роль: Андеррайтер</bpmn:text>
</bpmn:textAnnotation>
<bpmn:association sourceRef="Task_Review" targetRef="Annotation_Role" />
```

**Вариант C: Префикс в имени** (простая документация)
```xml
<bpmn:userTask name="[Финансы] Проверить заявку" />
```

### Когда дорожки ДОПУСТИМЫ ✅:

Lanes РАЗРЕШЕНЫ (и рекомендуются) для:

1. **Мульти-организационные процессы** - Разные компании/организации взаимодействуют
   - Пример: Клиент ↔ Поставщик ↔ Логистика
   - Каждая организация = отдельный Pool со своими lanes
   - Связаны через Message Flows

2. **Кросс-системное взаимодействие** - Независимые системы обмениваются сообщениями
   - Пример: E-commerce ↔ Платёжный шлюз ↔ Доставка
   - Каждая система = отдельный Participant

3. **Явное требование пользователя** - Пользователь конкретно просит lanes
   - Предупреди о снижении читаемости
   - Предложи альтернативу с role overlays

**Ключевое различие:**
- ❌ Одна организация, разные отделы → БЕЗ lanes (используй role overlays)
- ✅ Несколько организаций → С lanes (используй separate pools)

### Правило валидации:

```
✅ ПРАВИЛЬНО: Один pool без lanes, роли в атрибутах задач
❌ НЕПРАВИЛЬНО: Несколько lanes для отделов одной организации
⚠️  ДОПУСТИМО (с обоснованием): Lanes для разных компаний + message flows
```

**Запомни:**
> **"Happy path должен быть прямой линией, а не зигзагом по дорожкам."**

---

## ❌ Анти-паттерн: Логически Неполный Цикл (Loop без изменения состояния)

### 🔴 ПРОБЛЕМА: "Магический" цикл без явного изменения состояния

**НЕПРАВИЛЬНО:**
```
Gateway "Данные корректны?" (Комитет проверяет)
  ↓ Нет
Task "Получить дополнительные данные" (Исполнитель собирает)
  ↓
Gateway "Данные корректны?" (Комитет снова проверяет)

❌ ПРОБЛЕМА: Как комитет узнал, что данные обновлены?
❌ ПРОБЛЕМА: Кто и когда обновил документ?
❌ ПРОБЛЕМА: "Магический" переход без явного действия!
```

**ПРАВИЛЬНО:**
```
Gateway "Данные корректны?" (Комитет проверяет)
  ↓ Нет
Task "Получить дополнительные данные" (Исполнитель собирает)
  ↓
Task "Обновить документ и подать повторно" (Исполнитель обновляет)
  ↓
Gateway "Данные корректны?" (Комитет повторно проверяет)

✅ Явное действие: Исполнитель ОБНОВЛЯЕТ документ
✅ Явный триггер: Повторная ПОДАЧА на проверку
✅ Логика ясна: Комитет проверяет ОБНОВЛЕННУЮ версию
```

### ⚙️ ОБЯЗАТЕЛЬНАЯ Валидация для КАЖДОГО Цикла

При создании loop pattern (любой поток, идущий назад), ПРОВЕРЬ:

#### 1. ✅ **Изменение Состояния**

**Вопрос:** Что изменилось между итерациями?

- ❌ **ПЛОХО**: `Проверка → [Не валидно?] → Получить инфо → Проверка`
  - *Кто обновил данные? Магия!*

- ✅ **ХОРОШО**: `Проверка → [Не валидно?] → Получить инфо → Обновить запись → Проверка`
  - *Понятно: запись обновлена между проверками*

#### 2. ✅ **Передача Между Ролями**

**Вопрос:** Если участвуют разные роли, явна ли передача?

- ❌ **ПЛОХО**: `[Комитет Проверяет] → Не четко → [Заявитель Собирает Инфо] → [Комитет Проверяет]`
  - *Как комитет узнал, что заявитель закончил?*

- ✅ **ХОРОШО**: `[Комитет Проверяет] → Не четко → [Заявитель Собирает Инфо] → [Заявитель Подает Повторно] → [Комитет Проверяет]`
  - *Явная повторная подача инициирует новую проверку*

#### 3. ✅ **Условие Выхода**

**Вопрос:** Может ли цикл завершиться?

- ❌ **ПЛОХО**: Бесконечный цикл без выхода
- ✅ **ХОРОШО**: Цикл с счетчиком попыток или таймаутом

### 📋 Шаблон Правильного Цикла

```
Gateway_Решение (Роль A - проверяющий)
  ↓ Условие не выполнено
Task_ДействиеИсправления (Роль B - исполнитель)
  ↓
Task_ИзменениеСостояния (Роль B - обновление/повторная подача)
  ↓
Gateway_Решение (Роль A - повторная проверка)
```

### 💡 Примеры из Реальной Практики

#### Пример 1: Проверка уведомления

**❌ НЕПРАВИЛЬНО:**
```xml
<!-- Шаг 1.1.1 -->
<bpmn:userTask id="Task_Identify" name="Идентифицировать работу" />

<!-- Шаг 1.1.2 -->
<bpmn:exclusiveGateway id="Gateway_Clear" name="Уведомление четкое?">
  <bpmn:outgoing>Flow_Clear_Yes</bpmn:outgoing>
  <bpmn:outgoing>Flow_Clear_No</bpmn:outgoing>
</bpmn:exclusiveGateway>

<!-- Шаг 1.1.3 -->
<bpmn:userTask id="Task_GetInfo" name="Получить информацию" />

<!-- LOOP BACK - БЕЗ промежуточного обновления -->
<bpmn:sequenceFlow sourceRef="Task_GetInfo" targetRef="Gateway_Clear" />
```

**Проблема:** Между `Task_GetInfo` и `Gateway_Clear` нет явного действия обновления уведомления!

**✅ ПРАВИЛЬНО:**
```xml
<!-- Шаг 1.1.1 -->
<bpmn:userTask id="Task_Identify" name="Идентифицировать работу" />

<!-- Шаг 1.1.2 -->
<bpmn:exclusiveGateway id="Gateway_Clear" name="Уведомление четкое?">
  <bpmn:outgoing>Flow_Clear_Yes</bpmn:outgoing>
  <bpmn:outgoing>Flow_Clear_No</bpmn:outgoing>
</bpmn:exclusiveGateway>

<!-- Шаг 1.1.3 -->
<bpmn:userTask id="Task_GetInfo" name="Получить дополнительную информацию" />

<!-- Шаг 1.1.3a - НОВЫЙ ОБЯЗАТЕЛЬНЫЙ ШАГ -->
<bpmn:userTask id="Task_UpdateNotification"
               name="Обновить уведомление и подать повторно" />

<!-- LOOP BACK - ЧЕРЕЗ обновление состояния -->
<bpmn:sequenceFlow sourceRef="Task_GetInfo" targetRef="Task_UpdateNotification" />
<bpmn:sequenceFlow sourceRef="Task_UpdateNotification" targetRef="Gateway_Clear" />
```

**Решение:** Добавлен промежуточный task `Task_UpdateNotification`, который:
- ✅ Обновляет уведомление с полученной информацией
- ✅ Подает его повторно на проверку комитета
- ✅ Создает явный триггер для повторной проверки

#### Пример 2: Ревью документа

**❌ НЕПРАВИЛЬНО:**
```
Gateway "Документ одобрен?"
  → Нет → Task "Внести правки"
  → Gateway "Документ одобрен?" (loop)
```

**✅ ПРАВИЛЬНО:**
```
Gateway "Документ одобрен?"
  → Нет → Task "Внести правки"
  → Task "Отправить на повторное ревью"
  → Gateway "Документ одобрен?" (loop)
```

### 🔍 Правило Обнаружения

**Если видишь loop pattern и не можешь ответить на вопрос:**
> *"Что конкретно изменилось между итерациями?"*

**→ УВЕДОМИ ПОЛЬЗОВАТЕЛЯ!** (НЕ исправляй автоматически)

**КРИТИЧЕСКИ ВАЖНО: При обнаружении логически неполного цикла:**

🚫 **НЕ ДЕЛАЙ:**
- Автоматически добавлять недостающие задачи
- Изменять структуру процесса без одобрения пользователя
- Предполагать, каким должен быть недостающий шаг

✅ **ДЕЛАЙ:**
1. **Обнаружи**: Выяви loop pattern и отсутствие изменения состояния
2. **Документируй**: Объясни, что отсутствует и почему это проблема
3. **Сообщи**: Создай понятное сообщение пользователю с:
   - Текущей структурой (с пробелом)
   - Что отсутствует (task изменения состояния)
   - Почему это проблематично (магический переход)
   - Предлагаемое решение (пример того, что можно добавить)
4. **Дождись решения**: Пусть пользователь сам решит, нужно ли исправлять и как

**Пример сообщения пользователю:**
```
⚠️ Обнаружен логически неполный цикл

Текущая структура:
  Gateway "Четкое?" → Нет → Task "Получить инфо" → Gateway "Четкое?"

Проблема: Нет явного изменения состояния между итерациями.

Вопрос: Что происходит между получением информации и повторной проверкой?

Предлагаемое решение (на ваше усмотрение):
  Добавить task: "Обновить уведомление и подать повторно"

Хотите, чтобы я добавил эту задачу, или обработаем иначе?
```

### ✅ Контрольный Список для Циклов

Перед завершением процесса с циклами, проверь:

- [ ] Есть ли явный task, изменяющий проверяемое условие?
- [ ] Понятно ли, как данные обновляются между итерациями?
- [ ] Если участвуют разные роли, есть ли явная передача (handoff)?
- [ ] Есть ли условие выхода из цикла (max attempts, timeout)?
- [ ] Можно ли проследить happy path через цикл?

**Запомни:**
> **"Каждая итерация цикла должна изменять состояние процесса - иначе это бесконечный loop!"**

---

**Версия:** 1.1 (2025-12-09)
**Изменения v1.1:**
- Добавлено правило: НЕ исправлять автоматически, только уведомлять пользователя
- Пользователь принимает решение о добавлении недостающих шагов
